import React from 'react';

const Header = () => {
    return (
        <header className="header">
            <img src="logo.png" alt="Header" />
            
        </header>
    );
}

export default Header;
